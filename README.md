# Documentation-assistant
By using langchain creating documentation assistant. It uses techniques like embeddings, VectorDbs, Retrival, Memory, etc.
## Commands to run the project
### Install required files
```$pipenv install```
### Activate the virtual environment
```$pipenv shell```
### Run the main file
```$pipenv run python ingestion.py```